# karin-plugin-drawImages

`#draw` image generation plugin for Karin.

## Usage

- `#draw 提示词`：文生图
- `#draw 提示词` 并附带图片：图生图

## Config

插件包内提供 `config/config.yaml.example` 作为配置示例。

Web 配置面板会保存到运行时的 `@karinjs/karin-plugin-drawImages/config/config.yaml`。
